# LaTeX / PDF — not installable via pip
#
# Run once after pip install -r requirements.txt:
#   bash scripts/install_latex.sh
#
# This installs TinyTeX to ~/.TinyTeX (no sudo) and tlmgr packages:
#   moderncv needspace fancyhdr fontawesome5 luatexbase
#   textpos titlesec cite xltxtra xunicode realscripts metalogo
#   geometry hyperref xcolor fontspec microtype enumitem
#
# Optional .env:
#   LATEX_BIN_DIR=$HOME/.TinyTeX/bin/x86_64-linux
