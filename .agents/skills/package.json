{
  "name": "job-scrapers-workspace",
  "private": true,
  "workspaces": [
    "scraper-shared",
    "*-search/cli"
  ],
  "scripts": {
    "install:all": "bun install",
    "typecheck": "bun run --filter './*-search/cli' typecheck",
    "test": "bun test scraper-shared/tests && bun run --filter 'pracuj-cli' test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "bun-types": "1.3.14",
    "typescript": "^5.9.3"
  }
}
